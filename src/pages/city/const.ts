import { deviceWidth } from '@utils/scale';

export const ITEM_HEIGHT = 44; //item的高度
export const HEADER_HEIGHT = 36; //分组头部的高度
export const HEADER_ITEM_BETWEEN_WIDTH = 10; // 城市之间的距离

export const HEADER_ITEM_MARGIN_Y = 10;
export const HEADER_ITEM_WIDTH = Math.floor((deviceWidth - -32 - 100 - 2 * HEADER_ITEM_BETWEEN_WIDTH) / 3);

export const HEADER_ITEM_HEIGHT = 28;

export const HEADER_PADDING_BOTTOM = 10;
